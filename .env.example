# Optional. Without these the leaderboard falls back to an in-process store that
# works fine locally and forgets on restart — the game itself needs nothing.
#
# Create a database at https://console.upstash.com, then copy its REST url/token.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
